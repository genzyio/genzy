// Auto-generated by N1mbly Client CLI
import {
  {% if path != null %}Controller,{% endif %}
  {% for method in existingMethods %}{{ method }},
  {% endfor %}Query,
  Path,
  Body,
} from '@n1mbly/client';
import {
{% for name, fields in types %}
  {{name}},
{% endfor %}
} from "./types";

{% if path != null %}@Controller('{{ path }}'){% endif %}
export class {{ name }} {
{% for action in actions %} {{ methods }}
{% if action.httpMethod %}  @{{ action.httpMethod }}({% if action.httpMethod %}'{{ action.path }}'{% endif %}){% endif %}
  async {{ action.name }}({% for param in action.params %}{% if param.source == "query" %}@Query('{{ param.name }}'){% endif %}{% if param.source == "path" %}@Path('{{ param.name }}'){% endif %}{% if param.source == "body" %}@Body(){% endif %} {{ param.name }}: {{ param.typeAdapted.name }}{% if not loop.last %}, {% endif %}{% endfor %}): Promise<{{ action.resultType.name }}> {
    // @ts-ignore
    return null;
  }
{% endfor %}
}
