// Auto-generated by N1mbly Client CLI
using System.Collections.Generic;

namespace N1mbly.Models
{
{% for name, fields in types %}
  public class {{name}}
  {
    {% for field_name, props in fields %}public {% if props.$isArray %}List<{{props.type or props.$typeName}}>{% else %}{{props.type or props.$typeName}}{% endif %}{% if props.$isOptional and (props.type == "int" or props.type == "float" or props.type == "boolean") %}?{% endif %} {{ field_name | capitalizeFirstLetter }} { get; set; }
    {% endfor %}
  }
{% endfor %}
}
