import { Tabs as Containter, type TabsInstance } from "./tabs";
import { Tab, type TabProps } from "./tab";

const Tabs = {
  Containter,
  Tab,
};

export { Tabs };

export type { TabsInstance, TabProps };
