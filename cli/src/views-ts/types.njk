// Auto-generated by Nimbly Client CLI
{% if isServer %} import {
  Controller, Get, Post, Put, Delete, Query, Path, Body, string, int, float, boolean, type, Returns, ReturnsArrayOf 
} from "nimbly-api";{% endif %} 
{% for name, fields in types %} export class {{name}}
  {
  {% for field_name, type in fields %}{% if isServer %}@{{type}} {% endif %}{{field_name}} : 
    {% if type == "int" or type == "float" %}number{% else %}{{type}}{% endif %};
  {% endfor %}
  }
{% endfor %}
