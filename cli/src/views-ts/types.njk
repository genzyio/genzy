// Auto-generated by N1mbly Client CLI
import {
  string, int, float, boolean, type, arrayOf
} from "{% if isServer %}@n1mbly/api{% else %}@n1mbly/client{% endif %}";

{% for name, fields in types %} export class {{name}}
  {
  {% for field_name, type in fields %}@{{type}} {{field_name}}:
    {% if type == "int" or type == "float" %}number{% else %}{{type}}{% endif %};
  {% endfor %}
  }
{% endfor %}
