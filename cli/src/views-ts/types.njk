// Auto-generated by N1mbly Client CLI
import {
  string, int, float, boolean, type, arrayOf
} from "{% if isServer %}@n1mbly/api{% else %}@n1mbly/client{% endif %}";

{% for name, fields in types %} export class {{name}}
  {
  {% for field_name, props in fields %}
    {% if isServer %}@{{props.type}}({% if props.$isOptional %}true{% endif %}){% endif %}
    {{field_name}}{% if props.$isOptional %}?{% endif %}:
  {% if props.type == "int" or props.type == "float" %}number{% else %}{{props.type}}{% endif %} {% if props.$isOptional %}| undefined{% endif %};
  {% endfor %}
  }
{% endfor %}
