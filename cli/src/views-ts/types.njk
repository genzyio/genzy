// Auto-generated by Nimbly Client CLI
import { Controller, Get, Post, Put, Delete, Query, Path, Body, string, int, float, boolean, type, Returns, ReturnsArrayOf } from "nimbly-api";

{% for name, fields in types %} export class {{name}}
  {
  {% for field_name, type in fields %}@{{type}} {{field_name}} : 
    {% if type == "int" or type == "float" %}number{% else %}{{type}}{% endif %};
  {% endfor %}
  }
{% endfor %}
