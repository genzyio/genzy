// Auto-generated by Nimbly Client CLI
import {
  {% for method in existingMethods %} {{ methods }},{% endfor %}
} from 'nimbly-client';

export class {{ name }} {
  $nimbly = {{ $nimbly | dump | safe }}
{% for route in routes %} {{ methods }}
  async {{ route.methodName }}({% for pathParam in route.pathParams %}{{ pathParam }}{% if not loop.last or route.body %}, {% endif %}{% endfor %}{% if route.body %}body{% endif %}) {}
{% endfor %}
}
