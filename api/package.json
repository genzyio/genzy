{
  "name": "gn1mbly-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm run build:client && tsc && npm run copy-public-to-dist && node dist/src",
    "build": "npm run build:client && tsc && npm run copy-public-to-dist",
    "build:client": "cd ../client && npm run build && rm -rf ../api/public 2> /dev/null && cp -r dist ../api/public",
    "copy-public-to-dist": "cp -r public dist/src",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "2.8.5",
    "dotenv": "16.0.3",
    "express": "4.18.2",
    "moment": "2.29.4",
    "multer": "1.4.5-lts.1",
    "sqlite3": "5.1.6"
  },
  "devDependencies": {
    "@jest/globals": "29.0.3",
    "@types/cors": "2.8.13",
    "@types/express": "4.17.14",
    "@types/multer": "1.4.7",
    "jest": "29.0.3",
    "ts-jest": "29.0.3",
    "typescript": "4.9.3"
  }
}